using System;
using System.Collections;
using System.Collections.Generic;
using Presenter;
using Unity.Mathematics;
using UnityEngine;
using UnityEngine.AI;
using Random = UnityEngine.Random;


public class Builder : MonoBehaviour
{
    /*[SerializeField] private GameObject[] block = default;
    [SerializeField] private Transform[] posSpawn = default;
    private void Start()
    {
        if (View._Instance.countBlock > 0)
        {
           int obj = Random.Range(0, block.Length);
           int pos = Random.Range(0, posSpawn.Length);
           Instantiate(block[obj], posSpawn[pos].position, posSpawn[pos].rotation);
           View._Instance.countBlock--;
        }
        
    }*/
}
